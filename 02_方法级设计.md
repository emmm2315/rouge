# 方法级设计

> 依据 `src/main/java/com/phantomcorridor` 实际源码，列出核心类的方法签名、参数、返回值、前置/后置条件与设计落点。

## 1. 核心方法契约

| 类 | 方法签名 | 参数 | 返回 | 前置条件 | 后置条件 | 设计落点 |
| --- | --- | --- | --- | --- | --- | --- |
| `core.GameLoop` | `final void handle(long now)` | `now` 纳秒时间戳 | `void` | `lastNanos` 为 `-1`（首帧）或上一帧时间戳 | 累加器消费至 `< FIXED_DT`；调用 0~N 次 `update(FIXED_DT)`、恰好 1 次 `render(frameDelta)` | 固定 1/60s 步长循环，与刷新率解耦，`MAX_FRAME_TIME=0.25` 防死亡螺旋 |
| `core.GameLoop` | `abstract void update(double dt)` | `dt` 恒为 `FIXED_DT` | `void` | `dt == FIXED_DT` | 模型推进一个固定步长 | 子类 `GameController` 实现，映射为 `session.update` |
| `core.GameLoop` | `abstract void render(double frameDelta)` | 实际帧间隔 | `void` | — | 每帧渲染一次 | 渲染与逻辑分离，`frameDelta` 仅用于渲染 |
| `controller.SceneManager` | `void switchTo(GameState, Pane)` | 目标状态、目标界面 | `void` | `target` 已注册到根容器，否则抛 `IllegalArgumentException` | 唯一可见节点为 `target`；旧界面 `onExit`、新界面 `onEnter`；`state==nextState` | 统一场景切换入口 |
| `controller.SceneManager` | `void showOverlay(GameState, Pane)` | 目标状态、覆盖层 | `void` | `overlay` 已注册到根容器 | 底层视图保留可见但退出生命周期；覆盖层 `toFront()` 可见 | 暂停菜单用覆盖层 |
| `model.GameSession` | `void newRun(String, Difficulty)` | 开发种子（空串随机）、难度 | `void` | 无（可重复调用） | `player`/`worldShift` 重置；`floor=1`；`runCleared=false`；生成首层地图并放玩家到入口 | 单局聚合入口 |
| `model.GameSession` | `void update(dt, mx, my, tx, ty, attacking)` | 步长、移动向量、瞄准点、是否攻击 | `void` | `dt >= 0` | 依序推进：切界冷却→移动/碰撞→房间切换→房间内容→攻击→敌人 AI→清房判定→交互→能量回复 | 主循环唯一入口 |
| `model.GameSession` | `boolean tryShiftWorld()` | 无 | 是否切换成功 | 无 | 成功：目标世界存在安全落点、能量满、冷却结束 → 切界、清空能量、重置冷却、触发相位脉冲清弹；否则不变 | 双世界切换核心 |
| `entity.Player` | `void move(dx, dy, dt, minX, minY, maxX, maxY)` | 方向向量、步长、边界 | `void` | `dt>=0`，边界 `min<=max` | 方向向量非零时按 `speed*dt` 归一化移动并 `clamp` 到边界；影形态乘 `SHADOW_SPEED_MULTIPLIER` | 移动与边界碰撞 |
| `entity.Player` | `boolean takeDamage(int)` | 伤害值 | 是否实际扣血 | 无 | `damage>0` 且无敌时间未过且 `hp>0` 时扣血并置无敌时间；否则不变返回 `false` | 受击无敌（0.65s），防一帧重叠弹幕重复扣血 |
| `entity.Player` | `void toggleWorld()` | 无 | `void` | 无 | `currentWorld` 在 LIGHT/SHADOW 间翻转 | 形态切换原子动作 |
| `entity.Player` | `void restore/consumePhaseEnergy(double)` | 增减量 | `void` | 无 | 能量 `clamp` 到 `[0, PHASE_ENERGY_MAX]` | 相位能量上下限约束 |
| `effect.WorldShiftSystem` | `boolean tryShift(Player)` | 玩家 | 是否切换 | `player != null` | 冷却结束且能量满 → `toggleWorld()`+消耗能量+重置冷却+`pulsePending=true`；否则 `false` | 满能量/冷却规则单点 |
| `effect.WorldShiftSystem` | `boolean consumePulse()` | 无 | 是否有待发脉冲 | 无 | 消费 `pulsePending` 并复位 | 脉冲一次性触发 |
| `dungeon.MapGenerator` | `DungeonMap generate(long seed)` | 种子 | 房间节点图 | 无 | 生成 `DEFAULT_ROOM_COUNT` 个房间，首为入口、末为 Boss，无环带分支 | 可复现随机地图 |
| `dungeon.MapGenerator` | `static long parseSeed(String)` | 配置种子 | 长整型种子 | 无 | 空串→随机；数字→`parseLong`；文本→`hashCode` | 数字/文本种子均可复现 |
| `room.Room` | `boolean isDoorOpen(Direction)` | 方向 | 门是否开放 | 无 | `= cleared && hasDoor(direction)`，无副作用 | 门禁 = 清空 + 有门 |
| `room.Room` | `void setCleared(boolean)` | 是否清空 | `void` | 无 | 更新 `cleared` | 清空双界敌人后由 `GameSession` 写入 |
| `entity.Enemy` | `int takeHit(int attackDamage)` | 玩家攻击力 | 实际伤害 | `attackDamage >= 0` | `dealt = max(1, attackDamage - defense)`；`hp` 减少；驱动 hurt/death 动画 | 防御减免 + 「至少 1 点伤害」硬规则 |
| `entity.Enemy` | `static int scaledHitPoints(kind, floor, difficulty, scale)` | 敌种、层数、难度、倍率 | 生命值 | `scale>0` 否则按 1 | `max(1, round(base × 层数成长 × 难度倍率 × scale))` | 层数成长 + 难度倍率单点 |
| `combat.EnemySystem` | `void update(dt, Player, PlayerAttackSystem, RoomNavigationSystem)` | 步长、玩家、玩家攻击、导航 | `void` | `dt >= 0` | 结算玩家命中、推进每只敌人 AI（索敌/移动/开火/脱困/召唤）、更新弹体与召唤裂隙、清理死亡敌人 | 敌人 AI 主循环 |
| `combat.EnemySystem` | `boolean beginSummonIfDue(Enemy, Player)` | 首领、玩家 | 是否起手召唤 | `boss.isBoss()` | 三种时机任一满足且未达存活上限 → 起手召唤并置冷却；否则 `false` | 首领召唤三时机判定 |
| `combat.EnemySystem` | `static double detectionRange()` | 无 | 索敌半径 | 无 | 整房索敌开启时返回房间外接矩形对角线，否则返回固定半径 | 索敌范围规则单点 |

## 2. 设计落点说明

- **分层单向依赖**：`view` 只读 `GameSession` 只读接口绘制；`controller` 转发输入与场景切换；`model` 承载全部规则；`config` 集中所有魔法数值（[GameConfig.java](file:///d:/rouge/rouge/src/main/java/com/phantomcorridor/config/GameConfig.java)、[RoomConfig.java](file:///d:/rouge/rouge/src/main/java/com/phantomcorridor/config/RoomConfig.java)）。
- **「同世界才能命中」单点收敛**：命中与可见性裁决统一在战斗系统（`EnemySystem`/`PlayerAttackSystem`）完成，不在各处散落 `world ==` 比较。
- **固定步长可复现**：所有 `update(double dt)` 以 `FIXED_DT=1/60` 推进，便于单测（82 用例覆盖 14 个测试类）。
- **无「为写法而加的抽象」**：`GAME_OVER` 枚举保留但未启用、`combatActive` 字段注释保留但无读取点——二者均为「需求超前但当前闭环用不到」的占位，未强行为其增加场景或状态读写。
