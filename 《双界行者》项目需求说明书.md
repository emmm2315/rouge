# 《双界行者》项目需求说明书

> 项目状态基线：2026-09-10
> 本文同时记录产品目标与代码实现边界。标为“已实现”的内容以当前 `src/main` 为准；未标记的设计不应视为现有版本功能。

## 一、项目概述

### 1.1 名称与定位

- **名称：**《双界行者》（Dual World Walker）
- **类型：** 单机、俯视角、双世界切换、Roguelike、动作射击原型
- **平台：** Windows、macOS、Linux 桌面端
- **目标：** 在多层分支关卡中验证“切换世界以改变战斗与通路”的核心循环，并完成一局五层的可重复游玩流程。

### 1.2 技术约束

| 项目 | 当前方案 |
| --- | --- |
| 语言与运行时 | Java 21（Maven Compiler `--release 21`） |
| UI/渲染与媒体 | JavaFX 21.0.6（Controls + Media），Canvas 2D 自绘、MediaPlayer 循环播放 BGM |
| 构建 | Maven Wrapper（`mvnw` / `mvnw.cmd`） |
| 自动化测试 | JUnit Jupiter 5.12.1 |
| 设计原则 | 模型层不引用 JavaFX；控制器处理输入与生命周期；视图只负责布局与绘制 |

## 二、核心玩法

每个房间同时对应光之界（LIGHT）和影之界（SHADOW）。玩家只能伤害与自己处于同一世界的敌人，也只会受到同世界敌方攻击的伤害。因此，清理混合世界敌人的战斗房时，切界既是输出手段，也是规避风险与绕过相位墙的路径选择。

| 世界/形态 | 视觉和移动 | 已实现攻击 |
| --- | --- | --- |
| 光之界 | 金色系；基础移动速度 | 远程光弹，有速度、寿命与墙体碰撞 |
| 影之界 | 紫色系；移动速度乘以 1.15 | 近距离扇形斩击，独立冷却与攻击判定 |

### 2.1 相位切换

- 相位能量范围为 0～100，开局满值；仅满值可切换，切换后清空。
- 当前可通过时间自然回复、击杀敌人和拾取相位碎片恢复能量；战斗房内同样持续回复。
- 当前版本使用 **`Tab`** 切界（不是 Shift）。若目标世界位置被墙阻挡，会寻找最近的安全落点。
- 切换会触发相位脉冲，清除玩家附近敌方投射物，并有短暂的视觉反馈和切换冷却。

## 三、单局流程与当前边界

### 3.1 当前可玩流程

1. 登录或校验本地玩家档案后进入主菜单。
2. 点击「开始游戏」先在难度面板里选一档（简单 / 标准 / 困难 / 屌炸天），再以设置中的开发种子（可留空）开始新局；每层生成一张可复现的分支地图。
3. 在入口、战斗、奖励、商店、事件与 Boss 节点之间移动；战斗/Boss 节点生成敌人与门禁，其余节点提供资源。
4. 清空战斗房敌人后出口解锁，房间北侧出现宝箱；打开后奖励落在地面等待拾取。
5. 商店上架两件标价装备：`E` 选中并显示价格，再按一次 `E` 确认购买，走开自动取消选择。
6. 在 Boss 房与裂隙守望者战斗；其生命低于 50% 时由光界转为影界。战斗中它会适时召唤增援（见 §5.2），召唤物与首领同界。击败首领后房间中央出现传送门，走近按 `E` 进入下一层。
7. 一局共 5 层：每层的初始房间会提示「第 N 层」，HUD 常显层数与难度；穿过第 5 层的传送门即通关。

### 3.2 未完成流程

- 道具图鉴与道具效果、商店之外的资源消费出口尚未接入。
- 层间差异化敌人配置、更多 Boss 二阶段专属机制尚未实现。
- 未实现局内自动存档、联网与手柄。

## 四、地图与房间

### 4.1 地图规则

- `MapGenerator` 生成无环、带分支并以 Boss 节点收尾的房间图；数字和文本种子均可复现。
- 一局共 `GameConfig.TOTAL_FLOORS`（5）层，每层各生成一张地图：第 N 层的种子为本局种子加 `(N-1) × FLOOR_SEED_STEP`，因此每层地图互不相同但同一种子下可复现；玩家的生命、金币与装备跨层保留。
- 地图使用节点连接，而非固定网格。房间会记录已发现与已访问状态，小地图只显示已发现节点。
- 入口是无障碍安全房；Boss 房较大；其他房间从方形、矩形、L 形、十字形中选择，并按种子生成障碍与相位墙。
- 商店不会刷在距离入口不足 `RoomConfig.SHOP_MIN_DEPTH`（3）个房间的位置：开局既没有金币也没有选择余地。
- 出入口有方向与门禁状态。房间未清理时门为关闭状态，清理后开放。
- **房间内容只生成一次**：奖励、商店货架、宝箱与事件在第一次进入时按种子生成并保存在房间上，反复进出既不会重刷，也不会消失；已清空的怪物房再次进入不会重新刷怪。
- 小地图用细线连接相邻的已发现房间以表示连通性，当前房间可以走的门用亮线标出；房间以图标区分：`￥` 商店、`?` 事件、宝箱图标表示还有没打开的宝箱；已清空的战斗/首领房套绿框，还有未拾取物、未开宝箱或未触发事件的房间点金点。

### 4.2 房间类型与实现状态

| 类型 | 目标职责 | 当前实现 |
| --- | --- | --- |
| 入口 | 出生与安全区 | 已实现，无障碍、不刷敌人 |
| 战斗 | 清怪、开门、奖励 | 已实现清怪、开门与宝箱（开箱后奖励留在地面） |
| 奖励 | 直接获得资源 | 已实现金币与装备拾取物，内容只生成一次，奖励组以房间几何中心为原点摆放 |
| 商店 | 消耗金币购买道具 | 已实现两件标价装备、二次确认购买与离开范围取消选择 |
| 事件 | 祝福/诅咒选择 | 已实现一次性事件：治疗/相位碎片/装备，或伏击刷怪 |
| Boss | 层末战斗 | 已实现单 Boss 生成、光/影两套技能、跨界、裂隙闪现与适时召唤增援；击败后房间中央出现通往下一层的传送门 |

## 五、敌人与战斗

### 5.0 难度

点击主菜单「开始游戏」后先选难度，只改敌人的**基础属性**（生命与防御），与层数成长相乘叠加，玩家的生命、金币与装备不变：

| 难度 | 敌人基础属性倍率 | 说明 |
| --- | --- | --- |
| 简单 | 50% | 生命与防御减半，且防御更晚才出现（第二层仍为 0） |
| 标准 | 100% | 基准值 |
| 困难 | 150% | 生命与防御 ×1.5 |
| 屌炸天 | 200% | 生命与防御 ×2，防御上限同样翻倍 |

第 N 层、难度 D 下的敌人：生命 = `基础 × D × (1 + (N-1) × 0.25)`（四舍五入，至少 1），防御 = `floor((N-1) × 1 × D)` 且不超过 `round(3 × D)`。难度在选择时写入 `Settings`，整局保留，死亡重开仍沿用同一档。

同一个倍率也作用于**玩家攻击伤害**（`Difficulty.playerDamageMultiplier()`）：难度改的是双方数值的整体刻度，而不是单方面把敌人堆厚。否则「屌炸天」下敌人生命 ×2、玩家每击仍是 1～4 点，难度只会变成“同一个敌人多打一倍次数”的磨血，玩家自身的生命、金币与装备依然不变。

### 5.1 当前敌人池

| 分类 | 枚举标识 | 数量/生成规则 |
| --- | --- | --- |
| 普通敌人 | `LANTERN`、`WOLF`、`GOLEM`、`MAGE` | 战斗房随机生成 5～7 个，光/影交替分配 |
| 精英敌人 | `EXECUTIONER`、`BELL` | 后段战斗房有概率替换一只普通敌人 |
| Boss | `WATCHER` | Boss 房仅生成一名，生命值低于一半时从 LIGHT 转为 SHADOW；保留光界弹幕/审判/长矛与影界连斩/冲刺，另按阶段条件召唤增援 |

敌人随层数成长（`GameConfig`）：生命值为 `基础值 × (1 + (层数-1) × 0.25)`，防御为 `(层数-1) × 1` 并封顶 3；两者都要再乘难度倍率（见 §5.0）。防御只减免伤害，玩家每击至少造成 1 点，避免高层出现完全打不动的情况。

### 5.1.1 玩家生命与敌人伤害

玩家生命刻度为 **100 点**（`GameConfig.PLAYER_MAX_HP`），HUD 用一条**分段血条**显示（每 20 点一格，共 5 格），条内直接写出「当前 / 上限」，条长只占面板的一部分而不是铺满整行。

伤害**跟着招式走**，每一招在 `EnemySkill` 上都有自己的 `damage`：

| 档位 | 伤害 | 代表招式 |
| --- | --- | --- |
| 廉价小弹 | 3～4 | 灯魇暗针、法师扇面弹、鸣钟者钟波、守望者日光弹幕（16 发环形散射的单发） |
| 普通攻击 | 5～7.5 | 灯魇追踪光球、棱晶狼撕咬、傀儡地裂/践踏、法师镜面斩 |
| 精英与首领正经招式 | 8～10 | 处刑者长矛/盾击、鸣钟者光柱、守望者裂隙长矛 |
| 压轴重招 | 11～13 | 守望者裂隙斩、日审判、裂隙冲锋 |

没有单独配数值的招式（纯位移、召唤）用物种的**基础撞击伤害** `EnemyKind.attackDamage()`：小怪 4～5、傀儡/鸣钟者 8、处刑者 10、守望者 12。命中结算在 `Player.takeDamage(double damage, DamageType type)`：伤害可以是小数，打进生命值时向上取整，保证任何非零伤害都至少掉 1 点血。伤害按攻击所在世界带 `DamageType`（光/影/物理），渲染层据此给飘字上色。

### 5.1.2 护盾（临时生命值）

护盾是**一次性的额外血条**，不是减伤也不是无敌：

- **先挡伤害**：命中结算顺序固定为「先扣护盾，扣穿之后剩下的才进生命值」。一记 40 点重击打穿 30 点护盾后，剩下 10 点照常扣血（`Shield.absorb` 返回溢出量）。
- **不吃治疗**：生命恢复药剂只回生命，不会补护盾。
- **一层一条**：进新一层时（`GameSession.startFloor`）重置为一整条，层内打完就没有，不会像回血那样无限续航。
- **容量**：基础 `GameConfig.PLAYER_SHIELD_CAPACITY = 30`；装备「相位容器」额外 +12，换上时只同步容量、保留剩余量，不会白送一整条盾。
- **表现**：HUD 在血条下沿叠一条冷青色护盾条（长度按剩余点数的绝对值画，并夹在已填充的血条范围内），右侧标出剩余点数；角色身上还有一圈随剩余量变淡的相位护罩；被护盾挡下的那一下会飘出护盾色的「盾 N」。
- **配合无敌帧**：受击后 `PLAYER_HIT_INVULNERABILITY`（0.65 秒）内的后续命中被完全忽略，连护盾也不会被消耗。

### 5.2 已实现规则

- 敌人只在与玩家同世界时更新追击与攻击；非当前世界敌人保留在模型中但不造成碰撞。
- **索敌覆盖整个房间**：同界敌人只要与玩家同处一房（半径取房间外接矩形对角线，见 `GameConfig.ENEMY_AGGRO_WHOLE_ROOM`）就会锁定玩家并主动接近，房间内不再存在“站得太远所以完全不动”的敌人；锁定后持续追击，玩家切界或离开房间才重新索敌。
- **视线判定**：被墙体/障碍挡住时敌人不会开火，而是沿房间距离场（`RoomFlowField`，粗粒度 BFS）绕行，绕行方向按自身身位半径计算；距离场不可用时退回沿障碍切线的贴墙绕行。开火视线按弹体半径探测，避免体型大的敌人因为玩家脚下放不下自己的身位就判定“看不见”。
- **脱困兜底**：持续跟踪敌人“有没有更接近玩家”，连续 2 秒既没有进步、自身也没挪出 64 像素，就把它挪到最近一个距离场给得出方向的合法位置。脱困只在走得到的范围内挪动（仅当自身已经嵌进墙体时才允许就近弹出），不会隔墙把敌人搬到玩家身边。这条规则专门对付“把首领顶在墙角”的玩法——首领身位直径 92，一旦卡在走不出去的死角就既追不上也打不到。
- **守望者的短距裂隙闪现**：当它连续 2 秒无法接近玩家、或玩家在它自己的寻路场里根本不可达（躲进了它挤不进去的角落）时，撕开一道裂隙出现在玩家附近：在玩家周围 150～420 像素的环上搜落点，优先选落地就能直接打到玩家的位置，位移上限 560 像素；落地后 0.9 秒起手、两次之间 4 秒冷却。闪现是跨空间位移，允许越过墙体——这正是“玩家躲进死角”的解法；起终点会绘制扩散的裂隙特效，避免看起来像穿墙卡顿。
- **守望者的攻击与召唤优先级**：光界保留环形弹幕、审判标记与裂隙长矛，影界保留连斩与冲刺；它们按可用距离轮换，召唤不参与普通选技。召唤时机为：① 血量**首次跌破 70% / 35%**（无视冷却，但受存活上限约束，被挡住时不消耗阶段）；② 连续 **5 秒**打不到玩家（没有开火视线）；③ 场上无召唤物且本波之后已完成至少两次本体技能时补位（受 **12 秒**冷却限制）。首领进房后有 **6 秒**缓冲；玩家进入约 **160 像素**近身范围时，补位召唤让位给本体攻击，避免首领原地重复起手。
- **召唤先预警、后落地**：起手时在首领周围 130～250 像素的合法位置撕开 **1.2 秒**的裂隙，渲染层用素材包的 `spawn_circle` 预警圈、收拢的计时环与正在张开的 `summon_portal` 表现；成型后才放出召唤物并留下一圈迸发特效。裂隙优先开在首领与玩家之间，落点必须放得下召唤物身位、离玩家至少 **140 像素**、彼此间距至少 **110 像素**；找不到合法落点就不放怪，绝不把召唤物塞进墙里。
- **召唤物规则**：与首领同界（光界召唤灯灵/法师，影界召唤影狼/法师），层数与难度继承首领，生命按 **0.7 倍**打折，落地带 **0.8 秒**起手；场上同时最多 **4 只**（含未成型的裂隙）。首领倒下、或它半血跨界离开该界时（素材包规则 `clearOnBossWorldExit`），它的造物与未成型的裂隙一并溃散，**溃散不计击杀**——否则房间永远清不空、传送门不会出现，或者玩家会白拿金币与相位能量。
- 光界的守望者没有 `summon_*` 本体动作，光界召唤走它的 `transform`（相位收拢）动作，配合两界都有的 `summon_portal` 特效；影界召唤沿用 `summon_windup/release/recovery`。召唤与普通出招表分离：召唤只由上面三条判断触发，不参与随机选技。
- **首领房布局**：首领房的障碍物离房间边缘至少 144 像素、彼此间隔至少 112 像素（普通房分别是 96 / 28）。首领身位大，外圈通道必须宽到寻路网格采得到，否则它的距离场会把外圈判成不可达——那正是“能被顶在墙上”的根因。
- 开火距离分远程（620 像素）与近战（150 像素）两档，并各自保持站位距离（190 / 105 像素）；敌方弹体寿命由最大开火距离反推，保证最远处开火也能飞到玩家身上。
- **伤害按招式结算**：每个招式携带自己的伤害与相位属性，命中时由 `EnemySystem.resolvePlayerHit` 落到 `Player.takeDamage` 上——不再有“所有攻击都掉 1 点血”的统一结算（见 §5.1.1）。
- **受击飘字**：玩家挨打与敌人掉血共用一条通道（`EnemySystem.DamageFlash`），飘在角色/敌人头顶，按攻击所属相位上色（光界金、影界紫、无属性灰白），被护盾挡下时换成护盾色并写成「盾 N」；飘字是纯视觉残留，逻辑不依赖渲染帧。
- 玩家投射物和近战斩击仅命中同世界敌人。敌方攻击也携带世界属性，仅命中同世界玩家；所有攻击实体（投射物、扇形斩击、地面范围与冲刺落点）都保存瞄准角度，独立攻击特效按实际释放方向旋转。
- 敌人与敌方攻击会检测房间轮廓、墙体和路径；离开房间时会清理玩家的临时攻击。
- 敌人按自身身位半径选点生成（首领 46、精英 34、普通怪 27），不会出生在墙体内。
- **受击框与占位分离**：移动、寻路和障碍物碰撞继续使用脚下身位；玩家攻击则命中模型胸腹部的独立圆形受击框。普通、精英、首领的受击框半径分别为 35 / 45 / 62 像素，均小于对应模型轮廓，避免大型敌人只能打到脚底或擦到披风边缘就受伤。
- 敌人死亡会计入清房门禁并恢复相位能量。

### 5.3 后续战斗需求

当前敌人已具备待机、移动、前摇、释放、收招、受击、死亡、独立攻击特效与多种攻击图案；寻路仍为粗粒度距离场加局部贴墙绕行，玩家若站在只有自身身位放得下的窄缝里，敌人会聚集在缝口而无法开火。后续重点是补充更多二阶段专属机制、优化复杂地形下的寻路与继续平衡各物种数值。

## 六、UI 与操作

### 6.1 已实现界面

- 登录页：昵称与本地密码摘要；身份与设置分别持久化。
- 主菜单：开始游戏、道具图鉴占位页、设置、退出；登录与菜单共用双界动态背景。
- 游戏页：Canvas 房间与角色渲染、光/影 HUD、**100 点生命分段血条（含条内数值与护盾条）**、攻击充能、相位能量、当前层数、两界剩余敌人、金币和探索小地图。
- 首领房清空后房间中央绘制旋转的传送门（标注目标层数），走近即可交互；打通第五层后显示「穿越完成」通关界面（`R` 重开 / `M` 返回主菜单）。
- 交互提示：跟随角色显示 `E  拾取 生命恢复药剂 / 装备 晨曦法杖 / 打开宝箱 / 触发事件`；走到物品旁边还会在物品下方显示名称标签，商店商品常显「名称 + 价格」（买不起时变灰红），选中后高亮商品与价格牌并提示“再按 E 确认”。
- 探索小地图：细线表示房间连通性（当前房间可走的门用亮线），房间内用图标区分类型（`￥` 商店、`?` 事件、宝箱），并标出已清空的战斗/首领房（绿框）与还有东西可拿的房间（金点）。
- 小地图面板边长为 **187 px**（原 280 px 的约三分之二），当前房间始终居中；战斗期间隐藏，右上区域改显示两界剩余敌人数，清房后恢复小地图。
- 暂停页：Esc/P 暂停，冻结游戏循环并保留底层游戏画面；F11/Alt+Enter 切换全屏。
- **背景音乐**：登录/主菜单循环播放 `Ad astra`；开始游戏后的普通探索循环播放 `The Coming of the Future`；未清空 Boss 房循环播放 `Control's Wishes`。曲目切换采用短暂淡入淡出，音乐音量读取设置项。

### 6.2 操作表

| 动作 | 输入 |
| --- | --- |
| 登录/菜单确认 | Enter 或鼠标左键 |
| 移动 | WASD 或方向键 |
| 瞄准 | 移动鼠标 |
| 攻击 | 按住鼠标左键 |
| 交互（拾取/开箱/事件） | `E` |
| 商店购买 | `E` 选中（显示价格）→ 再按 `E` 确认；走开自动取消 |
| 光/影切换 | `Tab`（相位能量满值） |
| 暂停/继续 | Esc 或 P |
| 全屏 | F11 或 Alt+Enter |

## 七、工程结构

```text
com.phantomcorridor
├── App / Launcher                启动、场景编排和窗口事件
├── audio                         AudioManager：BGM 循环、淡入淡出与场景/战斗切换
├── config                        AppConfig、GameConfig、RoomConfig、Settings
├── controller                    登录、菜单、游戏输入与 SceneManager
├── core                          GameLoop、GameState
├── model
│   ├── GameSession               单局状态聚合
│   ├── dungeon                   MapGenerator、DungeonMap
│   ├── room                      Room、RoomLoot、RoomContentSystem、RoomLayout、Wall、RoomNavigationSystem、RoomFlowField
│   ├── entity                    Player、Enemy、EnemyKind、动画状态
│   ├── combat                    玩家攻击、敌人系统、攻击实体与投射物
│   └── effect                    WorldShiftSystem
├── util                          碰撞、随机数与资源加载
└── view                          登录、菜单、暂停、GameView、GameRenderer
```

资源位于 `src/main/resources/com/phantomcorridor/`：`sprites/` 保存玩家及怪物像素资源，`enemies/` 保存敌人图集与说明，`audio/` 保存 `menu.flac`、`exploration.flac`、`boss.flac` 三首循环 BGM，`ui/ui.css` 保存控件样式。

## 八、里程碑与验收状态

| 阶段 | 状态 | 实际交付 |
| --- | --- | --- |
| 第 1～2 天 | 完成 | JavaFX 工程、固定步长循环、场景生命周期、登录、本地档案、主菜单与设置 |
| 第 3 天 | 完成 | 玩家移动、鼠标瞄准、光弹、影刃、角色/基础 HUD 渲染 |
| 第 4 天 | 完成 | 相位能量、`Tab` 切界、冷却、安全落点、相位脉冲与清弹 |
| 第 5 天 | 完成 | 种子地图、房间布局/墙体/门禁、房间切换、探索小地图 |
| 第 6 天 | 主要完成 | 普通/精英敌人生成、同界 AI、伤害结算、清房门禁、基础 Boss 跨界 |
| 第 7 天 | 完成 | 房间内容（奖励/宝箱/事件）只生成一次、商店标价与二次确认购买、小地图清空与拾取提示 |
| 第 8 天 | 完成 | 五层推进：首领房传送门、层数提示、敌人按层提升生命与防御、通关结算 |
| 第 9～10 天 | 进行中 | 道具图鉴与道具效果、数值平衡与交付打包 |
| 补充迭代 | 完成 | 守望者多技能轮换、阶段召唤/裂隙闪现、模型中心受击框、攻击特效朝向、奖励房中心落点、三场景循环 BGM（见 §5.2、§6.1） |

### 验收清单（当前）

| 验收项 | 状态 |
| --- | --- |
| 能登录并从主菜单开始新局 | 已实现 |
| 随机且可复现的分支地图、战斗房和 Boss 房 | 已实现 |
| 商店不会刷在出生点附近 | 已实现（距入口至少 3 个房间） |
| 房间内容不会因反复进出而重刷或清空 | 已实现 |
| 已击败的怪物房再次进入不刷怪，并在小地图标记 | 已实现 |
| 在满相位能量下切换双世界 | 已实现（`Tab`） |
| 仅同世界敌人/攻击生效 | 已实现 |
| 远距离敌人也会主动索敌、接近并攻击玩家 | 已实现（索敌覆盖整房，含绕行寻路） |
| 敌人不会因为贴墙而永久卡死（含首领被顶在墙角） | 已实现（脱困兜底 + 首领房通道宽度 + 守望者裂隙闪现） |
| Boss 会适时召唤增援，且召唤可读可躲（先预警、后落地） | 已实现（血量阶段 / 打不到玩家 / 场上无召唤物，裂隙预警 1.2 秒） |
| Boss 近身时优先使用本体技能、攻击特效朝向正确 | 已实现 |
| 大型敌人的躯干可正常受击 | 已实现（独立模型中心受击框） |
| 奖励房奖励在房间中央生成 | 已实现 |
| 主菜单、探索、Boss 战 BGM 自动切换并循环 | 已实现（淡入淡出） |
| 清空双界敌人后开放出口 | 已实现 |
| 商店按标价出售、二次确认购买、离开范围取消选择 | 已实现 |
| 房间类型与状态在小地图上可辨认（连通线、`￥`/`?`/宝箱/传送门图标、已清空与有未拿取标记） | 已实现 |
| 回血道具明确提示为「生命恢复药剂」 | 已实现 |
| 玩家生命为 100 点并显示为分段血条（不占满整行、条内带数值） | 已实现 |
| 不同敌人的不同攻击造成不同伤害 | 已实现（伤害随招式配置，见 §5.1.1） |
| 护盾作为一次性临时生命，先于生命值被消耗 | 已实现（见 §5.1.2） |
| 一局五层、每层击败首领后出现可交互传送门并提示层数 | 已实现 |
| 四档难度可在开始游戏时选择，敌人基础属性按倍率生效 | 已实现（简单 50% / 标准 100% / 困难 150% / 屌炸天 200%） |
| 敌人生命值与防御随层数提升 | 已实现 |
| 死亡/通关结算并可回到主菜单 | 已实现（覆盖层 + `R` 重开 / `M` 返回） |
| 至少三种可拾取道具及光/影流派效果 | 未实现 |
| Boss 两阶段差异机制 | 部分实现（半血跨界，无独立技能） |

## 九、开发与测试

在 JDK 21+ 环境下使用 Maven Wrapper：

```powershell
.\mvnw.cmd javafx:run
.\mvnw.cmd test
```

测试目录当前覆盖输入、玩家状态与护盾、世界切换、地图、房间导航、房间内容与商店、敌人系统与按层/按难度成长、敌人伤害与中心受击框、首领召唤、层数推进、玩家攻击、敌方投射物、房间距离场及游戏会话等 17 个核心测试类。其中：

- 敌人索敌测试在多张真实种子地图上模拟 30 秒，校验每个同界敌人都能锁定玩家、主动接近并走到可开火的位置（首领中途召唤出的增援不计入这项断言）；
- 首领召唤测试校验：开场缓冲期内不召唤、跌破血量阶段时立刻召唤（且无视冷却，光界走 `transform`、影界走 `summon` 动作）、连续 5 秒打不到玩家时会喊增援、裂隙成型前不出现召唤物、落点站得下且离玩家有安全距离、存活上限与裂隙间距不被突破、召唤物与首领同界且比同层房间怪脆，以及首领倒下或跨界时造物连同未成型裂隙一并溃散、溃散不计入击杀；会话层用例另外校验召唤提示与 HUD 残敌数、以及首领倒下后房间立刻清空；
- 首领房与寻路测试校验 60 个种子下首领房内不存在“首领走不到”的落脚点（回归“把首领顶在墙角”的问题），并校验：和墙重叠的敌人会被脱困兜底挪出来、随后继续追击，脱困永远不会把敌人隔墙搬走，以及玩家躲进首领挤不进去的角落时守望者会靠裂隙闪现贴到身边、且冷却期间不会连闪；
- 房间内容测试真实走门进出商店与战斗房，校验货架不重刷、不因离开而清空、买走的商品不会刷回来、奖励房奖励组保持房间中心，以及二次确认与离开范围自动取消选择；地图测试额外校验 300 个种子里商店都不会刷在入口附近；
- 层数推进测试走完整流程：进入首领房 → 击败首领 → 传送门出现 → 按 `E` 进入下一层，连续五层后校验通关结算，并校验金币与装备跨层保留、敌人生命值与防御随层提升；
- 难度测试校验四档倍率作用在敌人基础属性上（傀儡基础 5 血：简单 3 / 标准 5 / 困难 8 / 屌炸天 10）、与层数成长叠加、简单难度更晚出现防御、难度跨层保留，以及同一批敌人在屌炸天下确实更肉；
- **敌人伤害测试**分三层校验：数值层（每一招都有自己的伤害、同一只怪的小招与重招不同、最重招式至少是最轻的 3 倍、召唤与纯位移招不造成正经伤害）；行为层（摆放指定物种与距离，真打中玩家后核对掉的血落在该物种该世界的招式数值集合里，并单独钉住傀儡地裂 6 点、灯魇光界 5 点 / 影界 4 点、棱晶狼扑咬 6 点——回归“所有攻击都掉 1 点血”的旧行为）；护盾层（第一下被护盾吃掉、生命值不掉且给出护盾色飘字，护盾被打穿后溢出伤害照常扣血、飘字随换层清理）；
- **护盾测试**校验纯数值行为：先挡伤害、30 点盾挨 20 点重击后溢出 18 点、盾碎后伤害全额穿透、回满一整条、改容量不白送护盾（变小时剩余量截断）、零伤害无副作用；
- **玩家测试**覆盖 100 点生命刻度与护盾联动：开局满血带满盾、护盾吃满一次伤害、护盾被打穿后溢出扣血、100 点重击只被盾挡 30 点、小数伤害向上取整至少扣 1 点、无敌帧内第二次命中连护盾都不消耗、药剂只回血不补盾（两瓶 = 20 点）、装备相位容器只抬容量不补剩余量。

> 最近一次合并保留了首领近身攻击优先、中心受击框、攻击特效朝向、奖励房中心落点与 BGM，并合入按招式伤害、护盾和受击飘字；将在本次冲突解决后用本地 JDK 21 + Maven Wrapper 重新运行完整测试验证。`.\mvnw.cmd javafx:run` 需要图形环境。
