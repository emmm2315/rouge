# 《双界行者》项目需求说明书

> 项目状态基线：2026-09-12
> 本文同时记录产品目标与代码实现边界。标为“已实现”的内容以当前 `src/main` 为准；未标记的设计不应视为现有版本功能。

## 一、项目概述

### 1.1 名称与定位

- **名称：**《双界行者》（Dual World Walker）
- **类型：** 单机、俯视角、双世界切换、Roguelike、动作射击原型
- **平台：** Windows、macOS、Linux 桌面端
- **目标：** 在多层分支关卡中验证“切换世界以改变战斗与通路”的核心循环，并完成一局五层的可重复游玩流程。

### 1.2 技术约束

| 项目 | 当前方案 |
| --- | --- |
| 语言与运行时 | Java 21（Maven Compiler `--release 21`） |
| UI/渲染与媒体 | JavaFX 21.0.6（Controls + Media），Canvas 2D 自绘、MediaPlayer 循环播放 MP3 BGM |
| 构建 | Maven Wrapper（`mvnw` / `mvnw.cmd`） |
| 自动化测试 | JUnit Jupiter 5.12.1 |
| 设计原则 | 模型层不引用 JavaFX；控制器处理输入与生命周期；视图只负责布局与绘制 |

## 二、核心玩法

每个房间同时对应光之界（LIGHT）和影之界（SHADOW），两界共享同一套敌人。敌人的生命、位置、灼痕、索敌、施法与弹幕连续保留，双方伤害不再按所属世界隔离。敌人的世界字段仅描述招式形态、素材和原生地形碰撞属性。

| 世界/形态 | 视觉和移动 | 已实现攻击 |
| --- | --- | --- |
| 光之界 | 金色系；基础移动速度 | 远程光弹，有速度、寿命与墙体碰撞 |
| 影之界 | 紫色系；移动速度乘以 1.15 | 近距离扇形斩击，独立冷却与攻击判定 |

### 2.1 相位切换

- 相位能量范围为 0～100，开局满值；任意能量均可切换；满值切界清空能量并强化，低能量切界保留能量但普通移速降低 35%，持续 0.3 秒。
- 当前可通过时间自然回复、击杀敌人和拾取相位碎片恢复能量；战斗房内同样持续回复。
- 当前版本使用 **`Tab`** 切界（不是 Shift）。若目标世界位置被墙阻挡，会寻找最近的安全落点。
- 满能量切换触发半径 118 的清弹脉冲与 0.3 秒无敌；普通切界不清弹、不新增无敌。统一切界冷却为 0.3 秒。成功切界取消攻击后摇及未完成攻击，不返还充能；失败切界不改变战斗状态。

### 2.2 共享敌人与灼痕协同

光攻击叠加灼痕（最多三层，六秒刷新）；影攻击消耗全部层数，每层增加 0.4 倍基础影伤系数并回复八点相位。物种分为弱光、弱影和光影破防，分别鼓励远程压制、近战收割和光影衔接。克制倍率、完整名单及伤害公式见《双界行者3.0改进项目书》。灼痕不因玩家切界或首领换形重置。

### 2.3 后续隐藏路线

形态限定入房触发隐藏出口属于 C 阶段规划，尚未实装。本次 A、B 不修改地图生成与连接。后续按进入时形态校验，允许正确形态重进补触发，奖励仅一次且不阻塞主线。

## 三、单局流程与当前边界

### 3.1 当前可玩流程

1. 登录或校验本地玩家档案后进入主菜单。
2. 点击「开始游戏」先在难度面板里选一档（简单 / 标准 / 困难 / 屌炸天），再以设置中的开发种子（可留空）开始新局；每层生成一张可复现的分支地图。
3. 在入口、战斗、奖励、商店、事件与 Boss 节点之间移动；战斗/Boss 节点生成敌人与门禁，其余节点提供资源。
4. 清空战斗房敌人后出口解锁，房间北侧出现宝箱；打开后奖励落在地面等待拾取。
5. 商店上架两件标价装备：`E` 选中并显示价格，再按一次 `E` 确认购买，走开自动取消选择。
6. 在 Boss 房与本层首领战斗；**每层的首领都不一样**：第 1 层固定「裂隙守望者」，第 2～5 层按本局种子从五个新首领（棱镜炮蟹 / 双镰螳爵 / 织巢蛛后 / 根冠古树 / 镜砂术士）里抽出互不重复的四个。所有首领生命低于 50% 时无伤害换形 1.2 秒、由光界转为影界。战斗中它会适时召唤增援（见 §5.2），召唤物与首领同界。击败首领后房间中央出现传送门，走近按 `E` 进入下一层。
7. 一局共 5 层：每层的初始房间会提示「第 N 层」，HUD 常显层数与难度；穿过第 5 层的传送门即通关。

### 3.2 未完成流程

- 道具图鉴与道具效果、商店之外的资源消费出口尚未接入。
- 首领的二阶段目前仍是「半血无伤害换形 + 换界后的另一套招式」，还没有各自专属的阶段机制。
- 未实现局内自动存档、联网与手柄。

## 四、地图与房间

### 4.1 地图规则

- `MapGenerator` 生成无环、带分支并以 Boss 节点收尾的房间图；数字和文本种子均可复现。
- 一局共 `GameConfig.TOTAL_FLOORS`（5）层，每层各生成一张地图：第 N 层的种子为本局种子加 `(N-1) × FLOOR_SEED_STEP`，因此每层地图互不相同但同一种子下可复现；玩家的生命、金币与装备跨层保留。
- 地图使用节点连接，而非固定网格。房间会记录已发现与已访问状态，小地图只显示已发现节点。
- 入口是无障碍安全房；Boss 房较大；其他房间从方形、矩形、L 形、十字形中选择，并按种子生成障碍与相位墙。
- 商店不会刷在距离入口不足 `RoomConfig.SHOP_MIN_DEPTH`（3）个房间的位置：开局既没有金币也没有选择余地。
- 出入口有方向与门禁状态。房间未清理时门为关闭状态，清理后开放。
- **房间内容只生成一次**：奖励、商店货架、宝箱与事件在第一次进入时按种子生成并保存在房间上，反复进出既不会重刷，也不会消失；已清空的怪物房再次进入不会重新刷怪。
- 小地图用细线连接相邻的已发现房间以表示连通性，当前房间可以走的门用亮线标出；房间以图标区分：`￥` 商店、`?` 事件、宝箱图标表示还有没打开的宝箱；已清空的战斗/首领房套绿框，还有未拾取物、未开宝箱或未触发事件的房间点金点。

### 4.2 房间类型与实现状态

| 类型 | 目标职责 | 当前实现 |
| --- | --- | --- |
| 入口 | 出生与安全区 | 已实现，无障碍、不刷敌人 |
| 战斗 | 清怪、开门、奖励 | 已实现清怪、开门与宝箱（开箱后奖励留在地面） |
| 奖励 | 直接获得资源 | 已实现金币与装备拾取物，内容只生成一次，奖励组以房间几何中心为原点摆放 |
| 商店 | 消耗金币购买道具 | 已实现两件标价装备、二次确认购买与离开范围取消选择 |
| 事件 | 祝福/诅咒选择 | 已实现一次性事件：治疗/相位碎片/装备，或伏击刷怪 |
| Boss | 层末战斗 | 已实现单 Boss 生成、光/影两套技能、跨界、裂隙闪现与适时召唤增援；击败后房间中央出现通往下一层的传送门 |

**装备栏规则（3 格）**：装备栏固定 3 格，不再自动挤掉旧装备。

- **同名可叠加**：同名装备允许同时占用多个槽位，数值增益按件数叠加（两把「晨曦法杖」= 光界攻击 +2，两个「行者心核」= 生命上限 +40%）。只有“形态切换”类效果（例如棱光三叉杖把光弹改成三向散射）仍只看“有没有”，装两把不会变成六向散射。
- **满栏替换**：装备栏满时拾取或购买装备不再默默生效，而是弹出换装面板。按 `1`、`2`、`3` 替换对应槽位；`ESC` 取消本次拾取，取消之后再按一次 `ESC` 才是暂停（面板关掉后不会自动追问，再按一次 `E` 可以重新考虑）。商店购买路径会在面板打开前先校验金币，买不起时保持“金币不足”提示、不弹面板。战斗期间不弹该面板。
- **战斗中不能动装备**：场上还有活着的敌人时，装备拾取、换装与丢弃全部被拒绝（`RoomContentSystem` 的 `inCombat` 参数，判定来自 `EnemySystem.isRoomCleared()`）。换装面板是模态的，在弹幕里停下来选槽位等于挨打；丢弃则可能把关键装备误扔进怪堆。金币、药剂等非装备拾取不受限制。提示会写明原因（「战斗中无法换装」/「战斗中无法丢弃装备」），装备留在原地等脱战后再捡。
- **装备不会消失**：新装备在玩家确认槽位之前一直躺在地上；被换下来的那一件落在**原拾取物的位置**。地面掉落物挂在房间状态上，离房后回来仍在，可以反复拾取或再次替换。
- **换下来的装备不是商品**：掉落物带 `shopGoods` 标记，只有货架上的商品才有价签。玩家换下或丢掉的装备落在商店房间里也按 `E` 直接捡回，不会再收一次钱。
- **主动丢弃**：非选择状态下按 `1`、`2`、`3` 丢弃对应槽位的装备。掉落物沿玩家朝向的小扇形错开摆放，落点保持在交互半径以内，所以丢在脚下的东西能立刻按 `E` 捡回来，连丢两件也不会叠成一点。
- **HUD**：左上角信息面板约 300×126（生命/攻击/相位 + 残敌/金币/盾量），面板下方是 285×126 的装备栏，三行竖排显示「编号 + 图标 + 名字（×N）」，鼠标悬停显示该装备效果说明。

## 五、敌人与战斗

### 5.0 难度

点击主菜单「开始游戏」后先选难度，只改敌人的**基础属性**（生命与防御），与层数成长相乘叠加，玩家的生命、金币与装备不变：

| 难度 | 敌人基础属性倍率 | 说明 |
| --- | --- | --- |
| 简单 | 50% | 生命与防御减半，且防御更晚才出现（第二层仍为 0） |
| 标准 | 100% | 基准值 |
| 困难 | 150% | 生命与防御 ×1.5 |
| 屌炸天 | 200% | 生命与防御 ×2，防御上限同样翻倍 |

第 N 层、难度 D 下的敌人：生命 = `基础 × D × (1 + (N-1) × 0.25)`（四舍五入，至少 1），防御 = `floor((N-1) × 1 × D)` 且不超过 `round(3 × D)`。难度在选择时写入 `Settings`，整局保留，死亡重开仍沿用同一档。

同一个倍率也作用于**玩家攻击伤害**（`Difficulty.playerDamageMultiplier()`）：难度改的是双方数值的整体刻度，而不是单方面把敌人堆厚。否则「屌炸天」下敌人生命 ×2、玩家每击仍是 1～4 点，难度只会变成“同一个敌人多打一倍次数”的磨血，玩家自身的生命、金币与装备依然不变。

### 5.0.1 战斗刻度（2026-09 重构）

旧刻度的两处硬伤：玩家基础攻击只有 1 点、首领只有几十点血，而防御最多能到 3 点——**防御比攻击还高**，于是每一击都被「至少 1 点」的兜底压成 1，武器系数（0.45～1.60）、伤害加成与防御三套系统全部失效。重构后的口径：

| 项目 | 旧值 | 新值 |
| --- | --- | --- |
| 玩家基础攻击 `BASE_ATTACK_DAMAGE` | 1 | **10** |
| 普通小怪基础生命 | 2～5 | **30～75**（灯魇 30 / 晶狼 45 / 法师 45 / 孢子 60 / 傀儡 75 / 甲虫 75 / 翼蝠 45） |
| 精英基础生命 | 9～10 | **130～140** |
| 首领基础生命 | 38～50 | **380～500**（守望者 500 / 炮蟹 440 / 螳爵 380 / 蛛后 420 / 古树 500 / 术士 400） |
| 防御（每层 +1，上限 3） | 相当于攻击的 100%～300% | 相当于一次基础攻击的 **10%～30%** |
| 攻击充能上限 / 每发回复 | 10 / 1.35 秒 | **16 / 0.5 秒** |

**敌人打到玩家**的伤害仍走「玩家 100 点生命」这套刻度（小怪 3～7、精英 8～10、首领 9～13），两套刻度各自内部自洽。难度倍率同时作用于敌人生命与玩家伤害，因此双方的比值不随难度漂移。

**实测节奏**（标准难度、无装备、脚本机器人自动索敌开火；机器人不会寻路绕墙，因此偏保守）：

| 楼层 | 清一间战斗房 | 击败首领 |
| --- | --- | --- |
| 第 1 层 | ≈ 15 秒 | ≈ 35 秒 |
| 第 3 层 | ≈ 37 秒 | ≈ 46 秒 |
| 第 5 层 | ≈ 64 秒 | ≈ 74 秒 |


### 5.1 当前敌人池

| 分类 | 枚举标识 | 数量/生成规则 |
| --- | --- | --- |
| 普通敌人（旧包） | `LANTERN`、`WOLF`、`GOLEM`、`MAGE` | 与 v2 小怪混在同一张池子里 |
| 普通敌人（v2） | `BEETLE`（铲角甲虫）、`SPORE`（行囊孢子）、`RAYBAT`（钩尾翼蝠） | 同上 |
| 精英敌人 | `EXECUTIONER`、`BELL` | 后段战斗房有概率替换一只普通敌人 |
| 首领 | `WATCHER`、`PRISM_CRAB`、`MANTIS`、`WEAVER`、`ROOTKING`、`HOURGLASS` | Boss 房仅生成一名，按层从 `BossRoster` 取 |

战斗房从 7 种普通怪里随机生成 **1～3 波，每波简单/标准 2～3 个、困难 2～4 个、屌炸天 3～4 个**（同一难度下单波数量不随层数增加；波间 1.2 秒，所有波次完成后才开门与出现宝箱），光/影交替分配；精英替换概率随层数提高（第 1 层 0%，此后 `0.20 + (层数-1) × 0.10`，上限 60%），所以深处的战斗房基本都会带上精英。

**每层一个不同的首领（`model.combat.BossRoster`）**：第 1 层固定「裂隙守望者」；第 2～5 层用**本局种子**把五个新首领洗成一份名册，按层取前四个——因此单局五层必然互不重复，同一种子可复现，换种子又能让缺席的那一只轮换上来。实现上有一处关键细节：`forFloor` 拿到的是**本层**的关卡种子（`本局种子 + (层数-1) × FLOOR_SEED_STEP`），必须先减回本局种子再洗牌，否则五层会各自独立地抽到同一个首领。

**v2 物种基础属性**（第一层 · 标准难度起点，层数成长与难度倍率仍按 §5.0 的公式叠加，不再次额外叠乘；数值刻度见 §5.0.1）：

| 物种 | 档位 | 基础生命 | 移速 px/s | 脚下身位 / 受击框 | 招牌机制 |
| --- | --- | ---: | ---: | --- | --- |
| 铲角甲虫 | 普通 | 75 | 72 | 27 / 35 | 直线顶撞（前摇正面减伤 25%）、掘进破土 |
| 行囊孢子 | 普通 | 60 | 55 | 27 / 35 | 孢子落在瞄准点，延迟绽放 / 脉冲雾区 |
| 钩尾翼蝠 | 普通 | 45 | 125 | 27 / 35 | 侧翼直线晶羽、俯冲 |
| 棱镜炮蟹 | 首领 | 440 | 54 | 46 / 62 | 炮击与路线封锁（弹体与爆炸共享一次命中、三点校射、三轮棱镜连装、蚀潮回弹） |
| 双镰螳爵 | 首领 | 380 | 125 | 46 / 62 | 二连斩、突刺、架镰反切（正面减伤 60%）、曦刃回旋 |
| 织巢蛛后 | 首领 | 420 | 66 | 46 / 62 | 召唤巢中小怪（第 3 层起带精英）、留门丝阵与交错夜网、网丝环射 |
| 根冠古树 | 首领 | 500 | 42 | 46 / 62 | 根刺与根带、生篱分庭（只挡路不伤害的临时地形）、冠落连播 |
| 镜砂术士 | 首领 | 400 | 82 | 46 / 62 | 幻象辨认、延迟攻击与镜门换位、千镜连照 |

敌人随层数成长（`GameConfig`）：生命值为 `基础值 × (1 + (层数-1) × 0.25)`，防御为 `(层数-1) × 1` 并封顶 3；两者都要再乘难度倍率（见 §5.0）。防御只减免伤害，玩家每击至少造成 1 点，避免高层出现完全打不动的情况。

### 5.1.1 玩家生命与敌人伤害

玩家生命刻度为 **100 点**（`GameConfig.PLAYER_MAX_HP`），HUD 用一条**分段血条**显示（每 20 点一格，共 5 格），条内直接写出「当前 / 上限」，条长只占面板的一部分而不是铺满整行。

伤害**跟着招式走**，每一招在 `EnemySkill` 上都有自己的 `damage`：

| 档位 | 伤害 | 代表招式 |
| --- | --- | --- |
| 廉价小弹 | 3～4 | 灯魇暗针、法师扇面弹、鸣钟者钟波、守望者日光弹幕（16 发环形散射的单发）、蛛后叉影丝梭、古树晶籽、暮孢脉冲 |
| 普通攻击 | 5～7.5 | 灯魇追踪光球、棱晶狼撕咬、傀儡地裂/践踏、法师镜面斩、甲虫顶撞/破土、孢子绽放、翼蝠俯冲 |
| 精英与首领正经招式 | 8～10 | 处刑者长矛/盾击、鸣钟者光柱、守望者裂隙长矛、炮蟹聚棱炮/闭钳震波、螳爵镰斩/反切、蛛后织光束丝/网环、古树根刺 |
| 压轴重招 | 11～13 | 守望者裂隙斩、日审判、裂隙冲锋、炮蟹三点校射/蚀潮封线、螳爵空架诱斩/折步掠杀、古树枯籽迟爆 |

没有单独配数值的招式（纯位移、召唤、筑墙）**显式写 0 并跳过伤害调用**——不能让“至少 1 点”的兜底把 0 变成伤害；只有确实没有配数值的普通撞击才回落到物种的**基础撞击伤害** `EnemyKind.attackDamage()`：小怪 4～5、傀儡/鸣钟者 8、处刑者 10、守望者 12。命中结算在 `Player.takeDamage(double damage, DamageType type)`：伤害可以是小数，打进生命值时向上取整，保证任何非零伤害都至少掉 1 点血。伤害按攻击所在世界带 `DamageType`（光/影/物理），渲染层据此给飘字上色。

### 5.1.2 护盾（临时生命值）

护盾是**一次性的额外血条**，不是减伤也不是无敌：

- **先挡伤害**：命中结算顺序固定为「先扣护盾，扣穿之后剩下的才进生命值」。一记 40 点重击打穿 30 点护盾后，剩下 10 点照常扣血（`Shield.absorb` 返回溢出量）。
- **不吃治疗**：生命恢复药剂只回生命，不会补护盾。
- **一层一条**：进新一层时（`GameSession.startFloor`）重置为一整条，层内打完就没有，不会像回血那样无限续航。
- **容量**：基础 `GameConfig.PLAYER_SHIELD_CAPACITY = 30`；装备「相位容器」额外 +12，换上时只同步容量、保留剩余量，不会白送一整条盾。
- **表现**：HUD 将护盾作为血条右侧的灰白附加段显示，不再挤占生命条文字；角色身上还有一圈随剩余量变淡的相位护罩；被护盾挡下的那一下会飘出护盾色的「盾 N」。
- **配合无敌帧**：受击后 `PLAYER_HIT_INVULNERABILITY`（0.65 秒）内的后续命中被完全忽略，连护盾也不会被消耗。

### 5.1.3 新增 15 件装备

新增装备沿用武器、披风、饰品三槽位，跨层保留，并使用独立高清像素贴图。**同名装备可以同时装备并占据多个槽位**，数值增益按件数叠加（见 §4.2「装备栏规则」）。**15 件装备的专属效果已全部实装**，数值与触发条件以《新增 15 件装备与攻击特效设计》为准：

| 类别 | 装备 | 已实装效果 |
| --- | --- | --- |
| 光界武器 | 棱光三叉杖 | -14°/0°/+14° 三发同步，每发 0.45D光，间隔 ×1.10 |
| | 贯日长杖 | 穿透三个敌人，依次 1.00/0.75/0.50D光，速度 ×1.35、寿命 ÷1.35 |
| | 折镜法球 | 首发 0.85D光，命中后弹射两次（0.55 / 0.35），链上不重复同一目标 |
| | 炽核权杖 | 0.70 倍速光核，命中/耗尽时半径 80 爆裂 1.10D光，无接触伤害 |
| 影界武器 | 环月镰 | 以出手位置为中心的 360° 环斩，半径 0.80R影、0.80D影 |
| | 归影双刃 | 往返影刃，最远 1.60R影，出程/返程各 0.55D影，出程遇墙折返 |
| | 夜坠重剑 | 40° 窄劈，距离 1.35R影、1.60D影，0.18 秒前摇，间隔 max(1.65, 0.30) |
| 双界武器 | 蚀界仪 | 光界两发 0.55D光（相隔 0.10 秒）；影界 0.75D影 + 0.22 秒后复斩 0.45D影 |
| 披风 | 曜纹披肩 | 光界受一次有效伤害降 30%，随后 6 秒冷却；影界不消耗就绪机会 |
| | 夜行披风 | 影界攻击实际释放后 0.45 秒内移速 +18%，不叠层、切界立即结束 |
| 饰品 | 凝光透镜 | 光界全部攻击 +25%，光界攻击间隔 ×1.15（影界无加成也无惩罚） |
| | 猎影牙饰 | 对当前生命 >70% 的敌人影伤 +25%，逐伤害包判定 |
| | 余震指环 | 每第 4 轮攻击追加震波：光界命中点 0.30D光/半径 60，影界 0.25D影/0.75R影 |
| | 相位陀螺 | 成功切界后 2 秒内新世界攻击间隔 ×0.85，重复获得只刷新时长 |
| | 行者心核 | 最大生命 +20%，装备不补血、卸下时当前生命截断到新上限 |

**结算口径**：单个伤害包的防御前伤害 = 基础伤害 × 段系数 ×（1 + 同类加成之和），多段**分别**减防御、不先合并（见《设计》§三）。武器系数是小数（0.45、0.55、1.60……），因此只在落到敌人身上那一刻才走 `Enemy.takeHit(int)` 的整数结算边界。

**攻击方案替换**：8 件武器替换的是整套攻击方案（弹体数、系数、穿透/弹射/爆裂、前摇与间隔），不是叠加属性。同界同时带多把时按固定优先级取一件（贯日长杖 > 炽核权杖 > 折镜法球 > 棱光三叉杖 > 夜坠重剑 > 归影双刃 > 环月镰 > 蚀界仪），保证手感可预期。

攻击方案在模型层结算，弹体、环斩、爆裂与延迟效果均受当前世界、墙体和房间边界约束；切界、离房和死亡会清理未完成的临时攻击（含蚀界仪的第二颗与复斩、重剑的前摇）。

### 5.2 已实现规则

- 敌人只在与玩家同世界时更新追击与攻击；非当前世界敌人保留在模型中但不造成碰撞。
- **索敌覆盖整个房间**：敌人只要与玩家同处一房（半径取房间外接矩形对角线，见 `GameConfig.ENEMY_AGGRO_WHOLE_ROOM`）就会锁定玩家并主动接近，房间内不再存在“站得太远所以完全不动”的敌人；锁定后持续追击，玩家切界继续索敌与施法，离房后清理房间战斗状态。
- **视线判定**：被墙体/障碍挡住时敌人不会开火，而是沿房间距离场（`RoomFlowField`，粗粒度 BFS）绕行，绕行方向按自身身位半径计算；距离场不可用时退回沿障碍切线的贴墙绕行。开火视线按弹体半径探测，避免体型大的敌人因为玩家脚下放不下自己的身位就判定“看不见”。
- **脱困兜底**：持续跟踪敌人“有没有更接近玩家”，连续 2 秒既没有进步、自身也没挪出 64 像素，就把它挪到最近一个距离场给得出方向的合法位置。脱困只在走得到的范围内挪动（仅当自身已经嵌进墙体时才允许就近弹出），不会隔墙把敌人搬到玩家身边。这条规则专门对付“把首领顶在墙角”的玩法——首领身位直径 92，一旦卡在走不出去的死角就既追不上也打不到。
- **守望者的短距裂隙闪现**：当它连续 2 秒无法接近玩家、或玩家在它自己的寻路场里根本不可达（躲进了它挤不进去的角落）时，撕开一道裂隙出现在玩家附近：在玩家周围 150～420 像素的环上搜落点，优先选落地就能直接打到玩家的位置，位移上限 560 像素；落地后 0.9 秒起手、两次之间 4 秒冷却。闪现是跨空间位移，允许越过墙体——这正是“玩家躲进死角”的解法；起终点会绘制扩散的裂隙特效，避免看起来像穿墙卡顿。
- **守望者的攻击与召唤优先级**：光界保留环形弹幕、审判标记与裂隙长矛，影界保留连斩与冲刺；它们按可用距离轮换，召唤不参与普通选技。召唤时机为：① 血量**首次跌破 70% / 35%**（无视冷却，但受存活上限约束，被挡住时不消耗阶段）；② 连续 **5 秒**打不到玩家（没有开火视线）；③ 场上无召唤物且本波之后已完成至少两次本体技能时补位（受 **12 秒**冷却限制）。首领进房后有 **6 秒**缓冲；玩家进入约 **160 像素**近身范围时，补位召唤让位给本体攻击，避免首领原地重复起手。
- **召唤先预警、后落地**：起手时在首领周围 130～250 像素的合法位置撕开 **1.2 秒**的裂隙，渲染层用素材包的 `spawn_circle` 预警圈、收拢的计时环与正在张开的 `summon_portal` 表现；成型后才放出召唤物并留下一圈迸发特效。裂隙优先开在首领与玩家之间，落点必须放得下召唤物身位、离玩家至少 **140 像素**、彼此间距至少 **110 像素**；找不到合法落点就不放怪，绝不把召唤物塞进墙里。
- **召唤物规则**：与首领同界（光界召唤灯灵/法师，影界召唤影狼/法师），层数与难度继承首领，生命按 **0.7 倍**打折，落地带 **0.8 秒**起手；场上同时存在的上限按层取（第 1～2 层 4 只、第 3～4 层 5 只、第 5 层 6 只，含未成型的裂隙）。首领倒下、或它半血跨界离开该界时（素材包规则 `clearOnBossWorldExit`），它的造物与未成型的裂隙一并溃散，**溃散不计击杀**——否则房间永远清不空、传送门不会出现，或者玩家会白拿金币与相位能量。
- **增援随层数升级**：每一波的只数按层取（第 1～2 层 2 只、第 3～4 层 3 只、第 5 层 4 只）；**从第 3 层开始，每一波的第一只是精英**（光界拉来执刑者、影界拉来鸣钟者，同样吃 0.7 倍生命折扣）；首领跌破**最后一个**血量阶段（35%）之后，每一波再追加一只——「血量越低叫得越凶」。第 1 层的召唤节奏与旧版完全一致（2 只 / 上限 4），既有测试因此仍然有效。
- 光界的守望者没有 `summon_*` 本体动作，光界召唤走它的 `transform`（相位收拢）动作，配合两界都有的 `summon_portal` 特效；影界召唤沿用 `summon_windup/release/recovery`。召唤与普通出招表分离：召唤只由上面三条判断触发，不参与随机选技。
- **首领房布局**：首领房的障碍物离房间边缘至少 144 像素、彼此间隔至少 112 像素（普通房分别是 96 / 96）。首领身位大，外圈通道必须宽到寻路网格采得到，否则它的距离场会把外圈判成不可达——那正是“能被顶在墙上”的根因。
- 开火距离分远程（620 像素）与近战（150 像素）两档，并各自保持站位距离（190 / 105 像素）；敌方弹体寿命由最大开火距离反推，保证最远处开火也能飞到玩家身上。
- **伤害按招式结算**：每个招式携带自己的伤害与相位属性，命中时由 `EnemySystem.resolvePlayerHit` 落到 `Player.takeDamage` 上——不再有“所有攻击都掉 1 点血”的统一结算（见 §5.1.1）。
- **受击飘字**：玩家挨打与敌人掉血共用一条通道（`EnemySystem.DamageFlash`），飘在角色/敌人头顶，按攻击所属相位上色（光界金、影界紫、无属性灰白），被护盾挡下时换成护盾色并写成「盾 N」；飘字是纯视觉残留，逻辑不依赖渲染帧。
- 玩家投射物和近战斩击仅命中同世界敌人。敌方攻击也携带世界属性，仅命中同世界玩家；所有攻击实体（投射物、扇形斩击、地面范围与冲刺落点）都保存瞄准角度，独立攻击特效按实际释放方向旋转。
- 敌人与敌方攻击会检测房间轮廓、墙体和路径；离开房间时会清理玩家的临时攻击。
- 敌人按自身身位半径选点生成（首领 46、精英 34、普通怪 27），不会出生在墙体内。
- **受击框与占位分离**：移动、寻路和障碍物碰撞继续使用脚下身位；玩家攻击则命中模型胸腹部的独立圆形受击框。普通、精英、首领的受击框半径分别为 35 / 45 / 62 像素，均小于对应模型轮廓，避免大型敌人只能打到脚底或擦到披风边缘就受伤。
- 敌人死亡会计入清房门禁并恢复相位能量。

### 5.2.1 v2 扩展包的招式表现（预警实体 / 临时地形 / 换位 / 幻象）

v2 的 36 个招式里，有三分之一不是「飞出去的弹体」，而是三类新实体，它们各自有独立的生命周期与清理规则：

- **预警与延迟判定区域（`model.combat.EnemyTelegraph`）**：扇面（含正前安全楔）、内/外半径环带留缺口、横带（缺口错开、可多段）、地面标记（真圈实线带齿、假圈虚线空心）。素材包的接入约定是「WINDUP 阶段创建无伤害预警实体、到 windupSeconds 触发一次 RELEASE」，因此这些判定**在起手那一刻就铺在地上**，玩家前摇期间读到的就是最终会爆的那块地面；多段招式（二连斩、两条根带、封线、三点校射）各自铺一份独立预警。预警几何一律按技能参数现画，而不是把通用模板当成判定范围（素材包文档亦如此要求）。
- **多段命中标识（`model.combat.HitIds` + `EnemySkill.HitPolicy`）**：全局 0.65 秒受击保护之外还需要一层去重，因为多段招式的间隔被刻意拉到 0.75～0.85 秒（不被保护帧吃掉）。策略分五档：整次施法只结算一次（`ONCE_PER_CAST`，炮蟹弹体与撞墙爆炸共享标识、封线三段共用）、每一下挥砍各一次（`ONCE_PER_SWING`）、每条带各一次（`ONCE_PER_BAND`）、每个标记各一次（`ONCE_PER_MARK`）、每枚弹体各一次（`ONCE_PER_PROJECTILE`）、每次脉冲各一次（`ONCE_PER_PULSE`）。
- **临时阻挡地形「根篱」（`model.combat.RootWall`）**：根冠古树的生篱分庭会竖起两段持续 4 秒的根篱，**只挡路、不伤害**。房间障碍是轴对齐矩形，根篱同样取整到最近的轴向并被裁进房间轮廓；中间永远留 140 像素通路，落点还要通过**可达性校验**（把房间铺成 40 像素粗网格，比较加上候选墙前后玩家能走到的格数，保留比例低于 55% 即判定会封死房间），校验不过就按素材包规则降级为 `cosmetic_only_nonblocking`——根篱照常长出来、用虚线描边标出，但不参与碰撞。根篱离玩家必须留出 100 像素，首选位置被玩家占住时会前后挪 70/140 像素再试。根篱通过 `RoomNavigationSystem.setTemporaryWalls` 生效，换房、换层、首领倒下都会清空。
- **换位（镜砂术士的镜门）**：起终点各开一道门，本体挪到 180～360 像素外的合法落点，落点必须站得下自己身位、离玩家至少 160 像素，并优先选「落地就能看见玩家」的位置；落地后固定 1.1 秒收招，不会变成贴脸瞬狙。
- **幻象**：术士的假影复用本体动画，但**不创建 `Enemy` 实体**——它们只进视觉层，半透明绘制并叠一圈空心虚线描边（灰度下也能分辨真假），因此不计清房、不产出奖励、不参与碰撞。
- **正面减伤（`Enemy.beginCastGuard`）**：铲角甲虫前摇期间甲壳正面减伤 25%、双镰螳爵架镰时正面减伤 60%。减免**带方向**：护角以起手时锁定的方向为中轴，绕到背后照常吃满伤害；收招时自动取消。
- **抛掷型弹体在瞄准点落地**：孢子的飞行弹体没有接触伤害，落点取「锁定目标距离」与「最大射程」中的较小者——否则从 200 像素外吐一口孢子会一路飞过玩家头顶、在射程尽头开花，预警圈画在谁也够不到的地方。玩家挪开就能躲，这正是它的玩法。
- **命中特效按物种 + 世界反推**（`EnemySystem.impactEffect`），与招式特效一样必须对应真实存在的素材目录；测试会逐个核对（见 §9）。
- **全图弹幕（三类做法）**：首领前摇整体压短之后（最长 1.6 → 1.2 秒以内），每个首领在两界都有一招覆盖整间房的弹幕：
  - **环形铺满**（`count` 发均匀铺满 360°，第 0 发正对锁定方向）——守望者日轮连射、蛛后网丝环射、古树冠落连播、术士千镜连照、鸣钟者钟波连鸣、螳爵夜刃连斩波、法师散辉连射；
  - **多轮连发**（`Pattern.BARRAGE`：轮间隔 + 每轮整体转角，让上一轮的缝隙被下一轮填上，玩家不能记住安全点站桩）；
  - **碰墙反弹**（`EnemySkill.bounces`）：弹体撞墙按法线折返，最多弹 3 次、寿命随反弹次数延长；反射方向在“反转 X / 反转 Y / 两者都反转”里挑第一个走得通的，三个都不通或次数用尽才消失，因此不会穿墙也不会在窄缝里无限弹射。守望者裂影回弹、炮蟹蚀潮回弹、蛛后夜网回弹、古树枯籽回响、术士镜砂乱弹、螳爵曦刃回旋都属这一类。
  - 判定的语义要分清：「相邻弹体夹角」是**每一跳**的角度（5 发 × 17.5° = 70° 宽的一排），打整圈必须用环形标记，否则相邻两发会隔整整一圈、整轮弹幕叠在同一个方向上飞出去（这个坑已经由测试钉住）。
- **多轮弹幕的弹体是“排进列表等发射”的**：`EnemyAttack` 的 `startupDelay` 让它先存在于列表里但不推进。因此「场上有多少弹幕」要按 `isActive()` 过滤，列表长度不等于弹幕数量。

### 5.3 后续战斗需求

当前 15 个物种（v1 七个 + v2 八个）已具备待机、移动、前摇、释放、收招、受击、死亡、独立攻击特效与多种攻击图案（弹体、扇形、环形、多轮弹幕、碰墙反弹、地面标记、预警几何、临时地形、换位、召唤），并各有自己的命中特效；寻路仍为粗粒度距离场加局部贴墙绕行，玩家若站在只有自身身位放得下的窄缝里，敌人会聚集在缝口而无法开火。后续重点是给首领补上各自专属的二阶段机制、继续优化复杂地形下的寻路，以及按实战击杀时间继续微调数值（当前实测节奏见 §5.0.1）。

## 六、UI 与操作

### 6.1 已实现界面

- 登录页：昵称与本地密码摘要；身份与设置分别持久化。
- 主菜单：开始游戏、道具图鉴占位页、设置、退出；登录与菜单共用双界动态背景。
- 游戏页：Canvas 房间与角色渲染、光/影 HUD、**100 点生命分段血条（含条内数值与右侧灰白护盾段）**、攻击充能、相位能量、两界剩余敌人、金币和探索小地图；层数与难度仅在进入每层时于屏幕中央短暂显示；场上出现首领时，屏幕顶端常显**首领名 + 形态 + 专属长血条**（一层一个不同的首领，玩家要能一眼看出自己在打谁），进 Boss 房时房间播报也会带上首领名。
- 首领房清空后房间中央绘制旋转的传送门（标注目标层数），走近即可交互；打通第五层后显示「穿越完成」通关界面（`R` 重开 / `M` 返回主菜单）。
- 交互提示：跟随角色显示 `E  拾取 生命恢复药剂 / 装备 晨曦法杖 / 打开宝箱 / 触发事件`；走到物品旁边还会在物品下方显示名称标签，商店商品常显「名称 + 价格」（买不起时变灰红），选中后高亮商品与价格牌并提示“再按 E 确认”。
- 探索小地图：细线表示房间连通性（当前房间可走的门用亮线），房间内用图标区分类型（`￥` 商店、`?` 事件、宝箱），并标出已清空的战斗/首领房（绿框）与还有东西可拿的房间（金点）。
- 小地图面板边长为 **187 px**（原 280 px 的约三分之二），当前房间始终居中；战斗期间隐藏，右上区域改显示两界剩余敌人数，清房后恢复小地图。
- 暂停页：Esc/P 暂停，冻结游戏循环并保留底层游戏画面；F11/Alt+Enter 切换全屏。
- **背景音乐**：登录/主菜单循环播放 `Ad astra`；开始游戏后的普通探索循环播放 `The Coming of the Future`；未清空 Boss 房循环播放 `Control's Wishes`。曲目切换采用短暂淡入淡出，音乐音量读取设置项。

### 6.2 操作表

| 动作 | 输入 |
| --- | --- |
| 登录/菜单确认 | Enter 或鼠标左键 |
| 移动 | WASD 或方向键 |
| 瞄准 | 移动鼠标 |
| 攻击 | 按住鼠标左键 |
| 交互（拾取/开箱/事件） | `E` |
| 商店购买 | `E` 选中（显示价格）→ 再按 `E` 确认；走开自动取消 |
| 光/影切换 | `Tab`（任意能量，满值强化） |
| 暂停/继续 | Esc 或 P |
| 全屏 | F11 或 Alt+Enter |

## 七、工程结构

```text
com.phantomcorridor
├── App / Launcher                启动、场景编排和窗口事件
├── audio                         AudioManager：BGM 循环、淡入淡出与场景/战斗切换
├── config                        AppConfig、GameConfig、RoomConfig、Settings
├── controller                    登录、菜单、游戏输入与 SceneManager
├── core                          GameLoop、GameState
├── model
│   ├── GameSession               单局状态聚合
│   ├── dungeon                   MapGenerator、DungeonMap
│   ├── room                      Room、RoomLoot、RoomContentSystem、RoomLayout、Wall、RoomNavigationSystem、RoomFlowField
│   ├── entity                    Player、Enemy、EnemyKind、动画状态
│   ├── combat                    玩家攻击、敌人系统、攻击实体与投射物、预警实体、临时根篱、
│   │                             召唤裂隙与召唤档案、每层首领名册、命中标识
│   └── effect                    WorldShiftSystem
├── util                          碰撞、随机数与资源加载
└── view                          登录、菜单、暂停、GameView、GameRenderer
```

资源位于 `src/main/resources/com/phantomcorridor/`：`sprites/` 保存玩家及怪物像素资源（含 `sprites/monsters/` 下的两套怪物素材与预警模板），`enemies/` 保存 v1 敌人图集与说明，`audio/` 保存 `menu.mp3`、`exploration.mp3`、`boss.mp3` 三首循环 BGM，`ui/ui.css` 保存控件样式。

**两套怪物素材的接入约定**：`monster_pack_v1` 与 `monster_expansion_v2` 共用同一套目录结构与渲染刻度——

```text
sprites/monsters/enemies/{物种id}/{light|shadow}/{动作}/{right|left|front|back}_NN.png
sprites/monsters/effects/{物种id}/{light|shadow}/{特效id}/right_NN.png
sprites/monsters/telegraphs/{light|shadow}/{形状}/01.png
```

本体画布：普通怪 256、精英 320、首领旧包 448 / v2 640，根锚点统一在画布高度的 87.5%；独立特效画布 384×384、中心锚点。渲染时整张画布按 `GameConfig.MONSTER_RENDER_SCALE`（0.70）等比缩放，因此各物种的相对体型由素材本身决定，而物理身位一律看 `EnemyKind.footRadius()`（普通 27 / 精英 34 / 首领 46），**透明画布不等于碰撞体积**。v2 只绘制了右向三分之四视角并镜像出左向，所以这八个物种按左右翻转表现朝向（`EnemyKind.hasFrontBackArt()` 为 false），不会被切到不存在的 front/back 帧上。

## 八、里程碑与验收状态

| 阶段 | 状态 | 实际交付 |
| --- | --- | --- |
| 第 1～2 天 | 完成 | JavaFX 工程、固定步长循环、场景生命周期、登录、本地档案、主菜单与设置 |
| 第 3 天 | 完成 | 玩家移动、鼠标瞄准、光弹、影刃、角色/基础 HUD 渲染 |
| 第 4 天 | 完成 | 相位能量、`Tab` 切界、冷却、安全落点、相位脉冲与清弹 |
| 第 5 天 | 完成 | 种子地图、房间布局/墙体/门禁、房间切换、探索小地图 |
| 第 6 天 | 主要完成 | 普通/精英敌人生成、共享战场 AI、伤害结算、清房门禁、基础 Boss 跨界 |
| 第 7 天 | 完成 | 房间内容（奖励/宝箱/事件）只生成一次、商店标价与二次确认购买、小地图清空与拾取提示 |
| 第 8 天 | 完成 | 五层推进：首领房传送门、层数提示、敌人按层提升生命与防御、通关结算 |
| 第 9～10 天 | 进行中 | 道具图鉴与道具效果、数值平衡与交付打包 |
| 补充迭代 | 完成 | 守望者多技能轮换、阶段召唤/裂隙闪现、模型中心受击框、攻击特效朝向、奖励房中心落点、三场景循环 BGM（见 §5.2、§6.1） |
| v2 怪物扩展 | 完成 | 3 种新小怪 + 5 个新 Boss 与 36 个招式全部接入（素材 2306 张帧图 + 22 张预警模板），新增预警实体 / 多段命中标识 / 临时根篱 / 换位 / 幻象五套机制，以及「每层一个不同首领」的名册（见 §5.1、§5.2.1） |
| 战斗数值与敌人强化 | 完成 | 伤害/生命刻度重构（基础攻击 10、小怪 30～75、精英 130～140、首领 380～500、防御回到 10%～30%）、充能上限与回复放宽、战斗房每层 +1 只且精英概率随层数提高、首领增援第 3 层起带精英且波次随层数与血量升级、首领前摇压到 1.2 秒以内、新增环形/多轮/碰墙反弹的全图弹幕（部分小怪同享）（见 §5.0.1、§5.2.1） |

### 验收清单（当前）

| 验收项 | 状态 |
| --- | --- |
| 能登录并从主菜单开始新局 | 已实现 |
| 随机且可复现的分支地图、战斗房和 Boss 房 | 已实现 |
| 商店不会刷在出生点附近 | 已实现（距入口至少 3 个房间） |
| 房间内容不会因反复进出而重刷或清空 | 已实现 |
| 已击败的怪物房再次进入不刷怪，并在小地图标记 | 已实现 |
| 任意能量切换双世界，满能量强化 | 已实现（`Tab`） |
| 仅同世界敌人/攻击生效 | 已实现 |
| 远距离敌人也会主动索敌、接近并攻击玩家 | 已实现（索敌覆盖整房，含绕行寻路；各物种站位一律不超过 190 像素的远程站位，否则敌人会在 190～站位之间“站着发呆”） |
| 敌人不会因为贴墙而永久卡死（含首领被顶在墙角） | 已实现（脱困兜底沿距离场树挪到前驱格、彻底走不动一秒后允许掉头、首领房通道宽度、首领裂隙闪现） |
| Boss 会适时召唤增援，且召唤可读可躲（先预警、后落地） | 已实现（血量阶段 / 打不到玩家 / 场上无召唤物，裂隙预警 1.2 秒） |
| Boss 近身时优先使用本体技能、攻击特效朝向正确 | 已实现 |
| 大型敌人的躯干可正常受击 | 已实现（独立模型中心受击框） |
| 奖励房奖励在房间中央生成 | 已实现 |
| 主菜单、探索、Boss 战 BGM 自动切换并循环 | 已实现（淡入淡出） |
| 清空双界敌人后开放出口 | 已实现 |
| 商店按标价出售、二次确认购买、离开范围取消选择 | 已实现 |
| 房间类型与状态在小地图上可辨认（连通线、`￥`/`?`/宝箱/传送门图标、已清空与有未拿取标记） | 已实现 |
| 回血道具明确提示为「生命恢复药剂」 | 已实现 |
| 玩家生命为 100 点并显示为分段血条（不占满整行、条内带数值） | 已实现 |
| 不同敌人的不同攻击造成不同伤害 | 已实现（伤害随招式配置，见 §5.1.1） |
| 战斗刻度可读：伤害与血量在同一量级、防御是有意义的百分比 | 已实现（玩家基础攻击 10，小怪 30～75 / 精英 130～140 / 首领 380～500，防御是一次基础攻击的 10%～30%，见 §5.0.1） |
| 楼层越深房间越挤、精英越常见 | 已实现（每层 +1 只，第 5 层 9～11 只；精英概率 0% → 60%） |
| 首领增援随层数升级并会带精英 | 已实现（第 3 层起每一波第一只必为精英，波次 2/2/3/3/4，上限 4/4/5/5/6，跌破 35% 再追加一只） |
| 首领招式不能“站着不动就能躲掉” | 已实现（全部首领前摇压到 0.5～1.2 秒，并新增覆盖整间房的弹幕） |
| 弹幕覆盖整间房（含碰墙反弹与多轮连发） | 已实现（环形铺满 8～12 发 / 多轮转角连发 / 最多 3 次碰墙反弹；部分小怪也会，见 §5.2.1） |
| 护盾作为一次性临时生命，先于生命值被消耗 | 已实现（见 §5.1.2） |
| 一局五层、每层击败首领后出现可交互传送门并提示层数 | 已实现 |
| 四档难度可在开始游戏时选择，敌人基础属性按倍率生效 | 已实现（简单 50% / 标准 100% / 困难 150% / 屌炸天 200%） |
| 敌人生命值与防御随层数提升 | 已实现 |
| 死亡/通关结算并可回到主菜单 | 已实现（覆盖层 + `R` 重开 / `M` 返回） |
| 至少三种可拾取道具及光/影流派效果 | 未实现 |
| Boss 两阶段差异机制 | 部分实现（半血无伤害换形 1.2 秒 + 换界后的另一套招式，无各首领专属阶段机制） |
| v2 扩展包的 3 种新小怪与 5 个新 Boss 连同特效全部接入 | 已实现（36 招 + 2306 张帧图 + 22 张预警模板，见 §5.1、§5.2.1） |
| 每层生成的首领与其余各层都不同 | 已实现（第 1 层固定守望者，第 2～5 层按本局种子抽四个互不重复的新首领） |
| 警告先完整出现、判定只结算一次 | 已实现（预警实体在起手时铺下，多段用显式命中标识去重） |
| 纯位移 / 召唤 / 筑墙不造成伤害 | 已实现（伤害显式为 0 并跳过伤害调用） |
| 临时地形不会把玩家封死 | 已实现（根篱永远留 140 像素通路，可达性校验不过则降级为不挡路的装饰） |
| 幻象不计清房、不产出奖励、不参与碰撞 | 已实现（假影只进视觉层，不创建敌人实体） |

## 九、开发与测试

在 JDK 21+ 环境下使用 Maven Wrapper：

```powershell
.\mvnw.cmd javafx:run
.\mvnw.cmd test
```

测试目录当前覆盖输入、玩家状态与护盾、世界切换、地图、房间导航、房间内容与商店、敌人系统与按层/按难度成长、敌人伤害与中心受击框、首领召唤、层数推进、玩家攻击、敌方投射物、房间距离场、游戏会话、v2 怪物扩展以及战斗数值重构等 **29 个核心测试类、249 个用例**。其中：

- 敌人索敌测试在多张真实种子地图上模拟 30 秒，校验每个同界敌人都能锁定玩家、主动接近并走到可开火的位置（首领中途召唤出的增援不计入这项断言）；
- 首领召唤测试校验：开场缓冲期内不召唤、跌破血量阶段时立刻召唤（且无视冷却，光界走 `transform`、影界走 `summon` 动作）、连续 5 秒打不到玩家时会喊增援、裂隙成型前不出现召唤物、落点站得下且离玩家有安全距离、存活上限与裂隙间距不被突破、召唤物与首领同界且比同层房间怪脆，以及首领倒下或跨界时造物连同未成型裂隙一并溃散、溃散不计入击杀；会话层用例另外校验召唤提示与 HUD 残敌数、以及首领倒下后房间立刻清空；
- 首领房与寻路测试校验 60 个种子下首领房内不存在“首领走不到”的落脚点（回归“把首领顶在墙角”的问题），并校验：和墙重叠的敌人会被脱困兜底挪出来、随后继续追击，脱困永远不会把敌人隔墙搬走，以及玩家躲进首领挤不进去的角落时守望者会靠裂隙闪现贴到身边、且冷却期间不会连闪；
- 房间内容测试真实走门进出商店与战斗房，校验货架不重刷、不因离开而清空、买走的商品不会刷回来、奖励房奖励组保持房间中心，以及二次确认与离开范围自动取消选择；地图测试额外校验 300 个种子里商店都不会刷在入口附近；
- 层数推进测试走完整流程：进入首领房 → 击败首领 → 传送门出现 → 按 `E` 进入下一层，连续五层后校验通关结算，并校验金币与装备跨层保留、敌人生命值与防御随层提升；
- 难度测试校验四档倍率作用在敌人基础属性上（傀儡基础 5 血：简单 3 / 标准 5 / 困难 8 / 屌炸天 10）、与层数成长叠加、简单难度更晚出现防御、难度跨层保留，以及同一批敌人在屌炸天下确实更肉；
- **敌人伤害测试**分三层校验：数值层（每一招都有自己的伤害、同一只怪的小招与重招不同、最重招式至少是最轻的 3 倍、召唤与纯位移招不造成正经伤害）；行为层（摆放指定物种与距离，真打中玩家后核对掉的血落在该物种该世界的招式数值集合里，并单独钉住傀儡地裂 6 点、灯魇光界 5 点 / 影界 4 点、棱晶狼扑咬 6 点——回归“所有攻击都掉 1 点血”的旧行为）；护盾层（第一下被护盾吃掉、生命值不掉且给出护盾色飘字，护盾被打穿后溢出伤害照常扣血、飘字随换层清理）；
- **护盾测试**校验纯数值行为：先挡伤害、30 点盾挨 20 点重击后溢出 18 点、盾碎后伤害全额穿透、回满一整条、改容量不白送护盾（变小时剩余量截断）、零伤害无副作用；
- **玩家测试**覆盖 100 点生命刻度与护盾联动：开局满血带满盾、护盾吃满一次伤害、护盾被打穿后溢出扣血、100 点重击只被盾挡 30 点、小数伤害向上取整至少扣 1 点、无敌帧内第二次命中连护盾都不消耗、药剂只回血不补盾（两瓶 = 20 点）、装备相位容器只抬容量不补剩余量；
- **v2 怪物扩展测试（`MonsterExpansionTest`，28 个用例）**分四层：数据层逐个钉住 8 个新物种的身位 / 档位 / 基础生命，以及 36 个招式的伤害、时序与命中策略，并校验纯召唤 / 换位 / 筑墙的伤害显式为 0；名册层在 300 个种子下校验「五层五个不同首领、第 1 层固定守望者、同种子可复现、换种子能覆盖全部五个新首领」，并校验每层 Boss 房真的生成名册上的那一个、新首领同样吃层数成长与难度倍率、半血换形播放 `transform` 并留下收招窗口；判定层校验冲撞警示带按实际行程改短（绝不超过承诺范围）、二连斩两段各结算 8 点、三点校射三个真标记各有独立标识、三镜校时只有 1 个真圈而错秒真影有 2 个、环带缺口与正前安全楔里确实不挨打、孢子落在瞄准点而不是飞过玩家头顶；机制层校验根篱进入导航层阻挡并按时枯萎、镜门换位落点合法且离玩家至少 160 像素、幻象不创建敌人实体、蛛后召唤自己的巢中小怪且不超上限、相位脉冲能打散玩家身边的可清弹幕、三种新小怪真的出现在战斗房刷怪池里，最后每个新首领在两界各打满一分钟以覆盖完整出招轮换。另有两条素材断言：招式表里写下的每个动作名、特效名、命中特效与预警模板都必须在资源里真的有对应帧——写错一个字母不会编译失败，只会在运行时静悄悄少半张图。

- **战斗数值重构测试（`CombatRebalanceTest`，16 个用例）**：钉住重构后的刻度（基础攻击 10、防御不超过一次基础攻击的 35%、最瘦的小怪也要挨 3 下以上、第 5 层的傀儡需要一管以上的充能、首领生命显著高于小怪）；战斗房数量随层数递增并封顶；精英第 1 层不出、第 5 层概率明显更高；首领增援的波次与存活上限随层数增长、跌破最后一个血量阶段再追加一只、第 3 层起第一只必为精英，并在真实召唤流程里验证精英确实出现了；所有首领前摇落在 0.5～1.2 秒之间；每个首领在两界都有一招覆盖整间房的弹幕；环形弹幕每一发朝向各自方向且不留下大于一个步长的空档（这条回归了「把环形当成扇面写，结果整轮弹幕叠在同一个方向飞出去」的真实缺陷）；多轮弹幕按轮数一波一波出现；反弹弹体真的用掉反弹次数并在撞墙后继续存活；部分小怪也确实学会了这些打法。

> 最近一次合并保留了首领近身攻击优先、中心受击框、攻击特效朝向、奖励房中心落点与 BGM，并合入按招式伤害、护盾和受击飘字；v2 怪物扩展与战斗数值重构均已在本地 JDK 21 + Maven Wrapper 下跑通完整测试（26 个测试类 / 238 个用例全绿）。`.\mvnw.cmd javafx:run` 需要图形环境。

- **BattleWaveTest** 覆盖 每个难度各 100 个种子下的 1～3 波分布、每波简单/标准 2～3 只、困难 2～4 只、屌炸天 3～4 只、波间等待与零时间冻结、最终清房、种子复现及事件/重置清理；会话测试验证波间无宝箱、全清后重进不刷新。

### 障碍布局与入场缓冲（2026-09-13）

战斗房扩大可用布局范围，尝试生成 6～9 组方柱、长掩体、L 形和 T 形掩体，事件房尝试 3～5 组。实际数量根据空间与连通性回退，不为了凑数封死通道。普通房障碍离区域边缘至少 96 像素，组间至少 96 像素；首领房保留 144 / 112 像素的宽通道。掩体以两界共有为主，搭配少量相位专属障碍，让绕行与切界各有作用。

布局完成后，用实际导航距离场检查两界中玩家及精英体型的连通性；首领房额外使用首领体型。检查失败时减少障碍。入口、奖励房和商店继续保留无障碍布局。

普通怪出生点距玩家至少 260 像素，避免与其他怪物重叠；出生后 0.8 秒显示“现身中”，不移动、不发起攻击。该规则覆盖初次入房、后续波次与事件伏击；首领及其召唤继续使用各自原有出场规则。
