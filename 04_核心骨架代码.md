# 核心骨架代码

骨架已满足「界面可显示、主循环可运转、状态可切换」，由以下四段串联：

```
Launcher.main ──▶ App.start(窗口+根容器+注册视图) ──▶ SceneManager(状态切换)
                                                        │
GameController(输入+循环) ──▶ GameLoop(固定步长) ──▶ GameSession.update ──▶ GameView.render
```

## 1. 界面可显示（窗口与场景注册）

[App.java](file:///d:/rouge/rouge/src/main/java/com/phantomcorridor/App.java#L63-L114) 创建 `1280×960` 主窗口、加载全局样式、把四个视图面板装入 `StackPane` 根容器，并默认进入登录界面：

```java
root.getChildren().addAll(loginView, mainMenuView, gameView, pauseView);
Scene scene = new Scene(root, AppConfig.VIEW_WIDTH, AppConfig.VIEW_HEIGHT);
stage.setScene(scene);
stage.show();
showLogin();  // sceneManager.switchTo(LOGIN, loginView)
```

## 2. 主循环可运转（固定步长）

[GameLoop.java](file:///d:/rouge/rouge/src/main/java/com/phantomcorridor/core/GameLoop.java#L68-L97) 基于 `AnimationTimer` 实现固定 `1/60s` 步长，`GameController` 匿名实现 `update/render`：

```java
this.loop = new GameLoop() {
    @Override protected void update(double dt) {
        session.update(dt, input.horizontal(), input.vertical(), aimX, aimY, attackHeld);
    }
    @Override protected void render(double frameDelta) {
        view.render(session, getSmoothedFps());
    }
};
```

`GameController.start()/stop()`（[GameController.java](file:///d:/rouge/rouge/src/main/java/com/phantomcorridor/controller/GameController.java#L64-L80)）随界面生命周期启停循环，暂停时 `loop.stop()` 冻结逻辑。

## 3. 状态可切换（场景与覆盖层）

[SceneManager.java](file:///d:/rouge/rouge/src/main/java/com/phantomcorridor/controller/SceneManager.java#L38-L78) 提供两个入口：

- `switchTo(GameState, Pane)`：普通场景切换（登录→主菜单→游戏）。
- `showOverlay(GameState, Pane)`：暂停覆盖层，底层画面保留但退出生命周期。

`App` 中四个方法（[App.java](file:///d:/rouge/rouge/src/main/java/com/phantomcorridor/App.java#L130-L153)）完成「登录 → 主菜单 → 游戏 → 暂停 → 恢复/返回」的状态流转。

## 4. 线程纪律

所有逻辑运行在 JavaFX 应用线程（`AnimationTimer` 回调线程），未引入独立工作线程。经核对界面线程内不存在耗时计算：

- `MapGenerator.generate` 为 `O(DEFAULT_ROOM_COUNT=9)` 轻量生成，毫秒级；
- 每帧 `GameSession.update` 仅推进有限实体的碰撞/伤害/AI，无阻塞 I/O、无网络、无重计算。

满足「没有把耗时计算放在界面线程」的验收要求；如后续接入存档 I/O，应移入后台线程并回派 `Platform.runLater` 更新界面。

## 5. 可运行性验证

```text
.\mvnw.cmd test
Tests run: 82, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

`javafx:run` 需要图形环境，未在本机无头会话启动；逻辑骨架由 82 个单测覆盖（含地图生成、房间导航、敌人系统、首领召唤、层数推进、会话状态等）。
