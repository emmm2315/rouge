# 最终回归结果（2026-09-14）

## 被测版本

- 分支：`main`
- 提交：`a7297e2`
- 系统：Windows 11
- JDK：25.0.3（项目目标编译版本 Java 21）

## 默认逻辑回归

```powershell
$env:JAVA_HOME = "D:\Program Files\Java\jdk-25.0.3"
.\mvnw.cmd test
```

```text
Tests run: 276, Failures: 0, Errors: 0, Skipped: 1
BUILD SUCCESS
```

默认跳过的是需要显式开启的 JavaFX 离屏预览测试。

## 含视觉预览回归

```powershell
.\mvnw.cmd test "-Dplayer.preview=true"
```

```text
Tests run: 276, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

该命令同时生成 `target/` 下的角色、技能、终结技和传送门预览图片。答辩归档时应保留 Maven 原始控制台输出和这些预览图片。

