# 核心方法 AI 生成与人工核对（怪物部分）

> 选取敌人（怪物）侧两个可独立验证的核心方法，给出 AI 生成并已人工核对、补充注释的最终实现。

## 1. 方法一：`Enemy.takeHit(int)` 及层数成长

**落点**：`model.entity.Enemy`，承载「防御减免 + 至少 1 点伤害」硬规则与「层数成长 × 难度倍率」的生命/防御计算（P0「子弹系统/敌人成长」）。

```java
/** 承受玩家一次攻击：先扣除防御，但每次至少造成 1 点伤害。 */
public int takeHit(int attackDamage) {
    int dealt = Math.max(1, attackDamage - defense);
    damage(dealt);
    return dealt;
}

/** 第 N 层、指定难度下的生命值：基础生命值 × 层数成长 × 难度倍率 × 实例倍率（四舍五入，至少 1）。 */
private static int scaledHitPoints(EnemyKind kind, int floor, Difficulty difficulty, double hitPointScale) {
    double floorScale = 1.0 + (Math.max(1, floor) - 1) * GameConfig.ENEMY_HP_GROWTH_PER_FLOOR;
    double scale = hitPointScale <= 0.0 ? 1.0 : hitPointScale;
    return Math.max(1, (int) Math.round(kind.hitPoints() * floorScale * difficulty.enemyStatMultiplier() * scale));
}

/** 第 N 层、指定难度下的防御：每层 +1 后乘难度倍率并向下取整，上限同样随难度缩放。 */
private static int floorDefense(int floor, Difficulty difficulty) {
    double multiplier = difficulty.enemyStatMultiplier();
    double raw = (Math.max(1, floor) - 1) * GameConfig.ENEMY_DEFENSE_PER_FLOOR * multiplier;
    int cap = (int) Math.round(GameConfig.ENEMY_DEFENSE_MAX * multiplier);
    return Math.min(cap, (int) Math.floor(raw));
}
```

### 人工核对记录

| 核对点 | 核对结论 | 是否需修正 | 处理 |
| --- | --- | --- | --- |
| 至少 1 点伤害 | `Math.max(1, attackDamage - defense)` 保证高层数下玩家攻击力不足时也不会完全打不动 | 否 | 需求 §5.1「玩家每击至少造成 1 点」已落实 |
| 防御减免 | `damage(dealt)` 内部 `hp = max(0, hp - amount)`，死亡不重复触发 | 否 | — |
| 层数成长 | `floorScale = 1 + (N-1)*0.25`，与需求 §5.0 一致 | 否 | — |
| 难度倍率叠加 | `enemyStatMultiplier()` 同时作用于生命与防御，与需求 §5.0 四档倍率一致 | 否 | `EnemyFloorScalingTest` 9 用例覆盖（简单 3/标准 5/困难 8/屌炸天 10） |
| 召唤物生命打折 | `hitPointScale=0.7` 时 `scaledHitPoints` 正确打折且下限 1 | 否 | `BossSummonTest` 覆盖 |

---

## 2. 方法二：`EnemySystem.beginSummonIfDue(Enemy, Player)`

**落点**：`model.combat.EnemySystem`，承载首领「三种适时时机」召唤判定（P0「Boss 战 · 召唤增援」）。

```java
private boolean beginSummonIfDue(Enemy boss, Player player) {
    if (!boss.isBoss() || activeCasts.containsKey(boss)) return false;
    int load = summonLoad();
    if (load >= GameConfig.WATCHER_SUMMON_MAX_ALIVE) return false;
    // ① 血量阶段：首次跌破 70% / 35% 时立刻召唤，无视冷却（被上限挡住时不推进阶段）
    boolean stageDue = boss.getSummonStage() < SUMMON_STAGE_HP.length
            && boss.getHp() <= boss.getMaxHp() * SUMMON_STAGE_HP[boss.getSummonStage()];
    // ② 逼出掩体：连续 5 秒打不到玩家
    boolean pressureDue = boss.getSummonPressure() >= GameConfig.WATCHER_SUMMON_PRESSURE_TIME;
    // ③ 补位：场上一个召唤物都不剩
    boolean reinforcementDue = load == 0;
    if (!stageDue && !pressureDue && !reinforcementDue) return false;
    // 只有非阶段召唤才受冷却限制；阶段召唤到点就放
    if (!stageDue && boss.getSummonCooldown() > 0.0) return false;
    EnemySkill summon = EnemySkill.forEnemy(EnemyKind.WATCHER, boss.getWorld()).stream()
            .filter(skill -> skill.pattern() == EnemySkill.Pattern.SUMMON).findFirst().orElse(null);
    if (summon == null) return false;
    if (stageDue) boss.advanceSummonStage();
    boss.setSummonCooldown(GameConfig.WATCHER_SUMMON_COOLDOWN);
    boss.resetSummonPressure();
    startCast(boss, player, summon);
    summonCallsSinceLastRead++;
    return true;
}
```

### 人工核对记录

| 核对点 | 核对结论 | 是否需修正 | 处理 |
| --- | --- | --- | --- |
| 三种时机判定 | 血量阶段（`SUMMON_STAGE_HP={0.70,0.35}`）/ 打不到压力（`5s`）/ 补位（`load==0`）任一满足即触发 | 否 | 需求 §5.2 一致 |
| 阶段召唤无视冷却 | `if (!stageDue && cooldown>0) return false` 只对非阶段召唤检查冷却 | 否 | `BossSummonTest` 覆盖「跌破阶段立刻召唤」 |
| 被上限挡住不推进阶段 | `if (stageDue) advanceSummonStage()` 只在真正起手时执行 | 否 | 防止阶段被白白吃掉 |
| 存活上限 | `load >= WATCHER_SUMMON_MAX_ALIVE(4)` 时不起手，`summonLoad()` 含未成型裂隙 | 否 | `BossSummonTest` 覆盖 |
| 不打断当前动作 | `activeCasts.containsKey(boss)` 时返回 false，避免打断正在播放的招式 | 否 | — |
| 召唤独立于随机出招表 | 召唤只由本方法判定，不混入 `chooseSkill` 的随机轮换 | 否 | 避免「满血白召唤」 |

**核对结论**：两个方法逻辑与需求一致，未发现需修正的功能缺陷；核对过程补充的说明性注释已保留在上述代码中。
